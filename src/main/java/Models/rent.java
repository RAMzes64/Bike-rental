package Models;

import java.sql.PreparedStatement;

public class rent {
/*
    public rent(int idBike, int idUser, String dateBegin, String dateEnd, int paycheck){
        String request = "INSERT INTO rent(id_user, id_shop_model, date, prepay) VALUES (?, ?, CONVERT( DATE, ?, 10), ?)";
        try{
            PreparedStatement statement = DataBaseSingleton.getConnection().prepareStatement(request);
            statement.setInt(1, idC);
            statement.setInt(2, idShopModel);
            statement.setString(3, date);
            statement.setInt(4,getPaycheck());
            System.out.println(statement.executeUpdate());
            if(statement != null) statement.close();
        }
        catch (Exception e){

        }
    }
    */
}
